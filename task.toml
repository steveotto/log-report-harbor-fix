artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a small JSON summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = "transform"
artifact_type = "generated_output_artifact"
expert_time_estimate_hours = 0.3
model_tested = "GPT-5.4"
agent_tested = "Terminus-2"
difficulty_explanation = "Parse a small access log and emit summary statistics."
solution_explanation = "Read each non-empty log entry, count total requests and distinct client IP addresses, identify the most frequently requested path, and write the results to /app/report.json."
verification_explanation = "Verify that /app/report.json is valid JSON with exactly the required keys and that total_requests, unique_ips, and top_path match the expected values derived from /app/access.log."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = false
mcp_servers = []
